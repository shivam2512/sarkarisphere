[build]
  functions = "netlify/functions"
  publish = "."

# Redirect /api/scrape → /.netlify/functions/scrape (transparent rewrite)
[[redirects]]
  from = "/api/scrape"
  to = "/.netlify/functions/scrape"
  status = 200
